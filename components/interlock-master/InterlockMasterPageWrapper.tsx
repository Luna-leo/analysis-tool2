"use client"

import { InterlockMasterPage } from "."

interface InterlockMasterPageWrapperProps {
  fileId: string
}

export function InterlockMasterPageWrapper(_props: InterlockMasterPageWrapperProps) {
  return <InterlockMasterPage />
}