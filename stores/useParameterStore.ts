import { create } from 'zustand'

// Simplified parameter store - parameters now come from CSV data sources only
export const useParameterStore = create(() => ({
  // Empty store - all parameter functionality moved to CSV data handling
}))